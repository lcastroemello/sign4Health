<body>
    <main>
    <h1>We in {{city}} all signed for organized international cooperation to provide global healthcare!</h1>
    {{#list}}
    <ul>
        <li>
        {{#if url}} <a href="{{url}}"> {{first}} {{last}}</a>
        {{else}} {{first}} {{last}}
        {{/if}} |

        {{#if age}}
        {{age}}
        {{/if}}
    </li>
    </ul>
    {{/list}}
    </main>
    <footer>
    <p>🌎 🌍 🌏</p>
    <a class="logout" href="/logout">  LOG OUT  </a>
    <a href="/myprofile"> See your profile </a>
</footer>
</body>
